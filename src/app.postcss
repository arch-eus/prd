@tailwind base;
@tailwind components;
@tailwind utilities;

html,
body {
	@apply h-full overflow-hidden bg-background;
}

@layer base {
  input:focus,
  textarea:focus,
  select:focus {
    outline: none;
  }
}